h1 投稿一覧

table.table.table-hover
  thead.thead-default
    tr
      th= Task.human_attribute_name(:content)
      th= User.human_attribute_name(:name)
      th=
  tbody
    - @tasks.each do |task|
      tr
        td= link_to task.content.truncate(10), task
        td= User.find_by(id: task.user_id).name
        td.text-right
          = link_to '編集', edit_task_path(task), class: 'btn btn-primary mr-3'
          = link_to '削除', task, method: :delete, data: { confirm: "こちらの投稿を削除します。よろしいですか？" }, class: 'btn btn-danger'
